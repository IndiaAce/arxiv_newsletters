name: fetch-ml-papers

on:
  schedule:
    # run once a day at 10:00 UTC (adjust as you like)
    - cron: "0 10 * * *"
  # lets you run it manually from the Actions tab too
  workflow_dispatch: {}

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: install deps
        run: |
          python -m pip install --upgrade pip
          pip install feedparser

      - name: run fetch script
        run: |
          python scripts/fetch_arxiv_papers.py

      - name: commit and push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add daily_papers LATEST_PAPERS.md
            git commit -m "update daily ml paper digest $(date -u +%F)"
            git push
          else
            echo "no changes to commit"
          fi
